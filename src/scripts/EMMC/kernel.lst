uuu_version 1.5.21

# @_target              | kernel

# burn kernel
FB: ucmd setenv fastboot_buffer ${loadaddr}
FB: download -f _target
FB[-t 60000]: ucmd fatwrite mmc ${emmc_dev}:1 ${fastboot_buffer} zImage ${fastboot_bytes}
FB: Done